[project]
name = "robovision"
version = "0.1.0"
description = "RoboVision — bilingual AI vision demo for secure product monitoring and loading verification (RoboFounders)"
readme = "README.md"
requires-python = ">=3.10,<3.14"
# Dependencies live in requirements.txt so Streamlit Cloud uses a single installer path.
dependencies = []
